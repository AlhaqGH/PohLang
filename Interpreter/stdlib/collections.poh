# Simple collections stdlib for system import
Make function head with xs set to List contains
    If length(xs) is greater than 0
        Return xs at 0
    End
    Return nothing
End

Make function tail with xs set to List contains
    If length(xs) is greater than 1
        # Build from 2..end
        Return xs
    End
    Return List contains
End
