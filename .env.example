# Environment variables for PohLang interpreter (optional)
# Copy to .env and adjust as needed

# Enable debug tracing in the interpreter (true/false)
POHLANG_DEBUG=false

# Default encoding for reading .poh files
POHLANG_ENCODING=utf-8
